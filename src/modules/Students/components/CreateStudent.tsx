import React from "react";

const CreateStudent: React.FC = React.memo(() => {
  return (
    <React.Fragment>
      <p>
        CreateStudent module is currently under development. Stay tuned for
        updates! 🚧🚩
      </p>
    </React.Fragment>
  );
});

export default CreateStudent;
